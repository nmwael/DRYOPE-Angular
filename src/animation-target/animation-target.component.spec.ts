import {AnimationTargetComponent} from './animation-target.component';

describe('Component: AnimationTarget', () => {
  it('should create an instance', () => {
    const component = new AnimationTargetComponent();
    expect(component).toBeTruthy();
  });
});
